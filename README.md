# port.tools python library
Keep update dynamic ip remotely

Run to perform unit test:
    python setup.py pytest
Build library:
    python setup.py bdist_wheel

Install:
    pip install /path/to/wheelfile.whl